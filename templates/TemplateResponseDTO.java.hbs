package {{dominio}}.{{projectName}}.application.dto;

/**
 * Data Transfer Object for {{recurso}} response.
 */
 @Data
public class {{recurso}}sResponseDTO {

    private Long id;
    private String name;

    // Add more fields as needed

    // Constructor, getters and setters
    public {{recurso}}sResponseDTO() {
    }
}
